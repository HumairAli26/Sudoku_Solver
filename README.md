# CodeAlpha_Sudoku_Solver
A C++ code that takes a sudoku's input in 9 lines where the empty boxes are showed by placing a 0. It checks row wise, column wise and then in a 3x3 box so that no number is repeated in these and then gives us the solution of the sudoku in a tabled order,
